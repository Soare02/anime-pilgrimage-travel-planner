# 路线规划模块 — 代码对应

> 路线规划模块主要解决两个问题：一是把地点分配到不同天数，二是确定每天内部的访问顺序。采用"空间聚类+最近邻排序+模型文本生成"的组合方式实现。

---

## 一、空间聚类 — Turf.js K-means

**文件：** `src/utils/routePlanner.js:22-50`

**描述：** 根据地点经纬度分成若干组，分组数量与用户输入的天数一致，让空间距离较近的地点尽量出现在同一天。

```js
const features = validPoints.map(p => turf.point([p.geo[1], p.geo[0]], { id: p.id }))
const collection = turf.featureCollection(features)

let clusters
try {
  clusters = turf.clustersKmeans(collection, { numberOfClusters: days })  // ← 聚类
} catch (e) {
  const sorted = nearestNeighborSort(validPoints, center)
  const totalDistance = calculateTotalDistance(sorted)
  return [{
    day: 1, points: sorted, distance: totalDistance, pointCount: sorted.length
  }]
}

// 按聚类结果分组
const clusterGroups = {}
clusters.features.forEach(f => {
  const cluster = f.properties.cluster
  if (!clusterGroups[cluster]) clusterGroups[cluster] = []
  const pointId = f.properties.id
  const originalPoint = validPoints.find(p => p.id === pointId)
  if (originalPoint) clusterGroups[cluster].push(originalPoint)
})

// 按各簇质心到中心点的距离排序（近的排前面）
const clusterEntries = Object.entries(clusterGroups).sort((a, b) => {
  const centerA = calculateClusterCenter(a[1])
  const centerB = calculateClusterCenter(b[1])
  const distA = turf.distance(turf.point([center[1], center[0]]), turf.point([centerA[1], centerA[0]]))
  const distB = turf.distance(turf.point([center[1], center[0]]), turf.point([centerB[1], centerB[0]]))
  return distA - distB
})
```

**关键行：** `turf.clustersKmeans(collection, { numberOfClusters: days })` — 分组数 = 用户输入天数

---

## 二、最近邻排序 — 每日内访问顺序

**文件：** `src/utils/routePlanner.js:80-121`

**描述：** 从当天的一个起始点出发，每次选择距离当前点最近的未访问地点，直到该组地点全部排序完成。

```js
function nearestNeighborSort(points, startPoint) {
  if (points.length <= 1) return [...points]

  const sorted = []
  const remaining = [...points]

  // 从起点出发，找最近的点作为第一个
  let nearestIdx = 0
  let minDist = Infinity
  remaining.forEach((p, idx) => {
    const dist = turf.distance(
      turf.point([startPoint[1], startPoint[0]]),
      turf.point([p.geo[1], p.geo[0]])
    )
    if (dist < minDist) { minDist = dist; nearestIdx = idx }
  })
  sorted.push(remaining.splice(nearestIdx, 1)[0])

  // 每次从当前点出发，找最近的未访问点
  while (remaining.length > 0) {
    const current = sorted[sorted.length - 1]
    let nearestIdx = 0
    let minDist = Infinity
    remaining.forEach((p, idx) => {
      const dist = turf.distance(
        turf.point([current.geo[1], current.geo[0]]),
        turf.point([p.geo[1], p.geo[0]])
      )
      if (dist < minDist) { minDist = dist; nearestIdx = idx }
    })
    sorted.push(remaining.splice(nearestIdx, 1)[0])
  }

  return sorted
}
```

**核心逻辑：** `while (remaining.length > 0)` 循环内，每次从 `sorted[sorted.length - 1]`（当前点）出发，用 `turf.distance` 找最近的未访问点。

---

## 三、入口函数 — planRoute（聚类 + 排序 编排）

**文件：** `src/utils/routePlanner.js:3-78`

**描述：** 整合聚类和排序，返回结构化路线结果（每日地标数组）。

```js
export function planRoute(points, days, center) {
  if (points.length === 0) return []
  if (days <= 0) days = 1
  if (days > points.length) days = points.length

  const validPoints = points.filter(p => p.geo && p.geo.length === 2)
  if (validPoints.length === 0) return []

  // 单天 → 直接最近邻排序
  if (days === 1) {
    const sorted = nearestNeighborSort(validPoints, center)
    const totalDistance = calculateTotalDistance(sorted)
    return [{ day: 1, points: sorted, distance: totalDistance, pointCount: sorted.length }]
  }

  // 多天 → 先聚类再每簇排序
  // ...（聚类代码见第一部分）...

  const result = clusterEntries.map(([_, clusterPoints], index) => {
    const sortedPoints = nearestNeighborSort(clusterPoints, center)
    const totalDistance = calculateTotalDistance(sortedPoints)
    return {
      day: index + 1,
      points: sortedPoints,
      distance: totalDistance,
      pointCount: sortedPoints.length
    }
  })

  return result
}
```

---

## 四、模型文本生成 — 拼装提示词 + 调用 LLM

**文件：** `src/utils/api.js:118-158`

**描述：** 把每天的地点名称、坐标和相关说明整理成提示词，发送给大语言模型。模型返回的 Markdown 文本仅用于展示，地图路线以算法结果为准。

```js
export async function generateAIRoute(days, landmarks, aiConfig, onChunk) {
  const { url, apiKey, model } = aiConfig
  const isAgent = url.includes('/api/agent')

  // 拼装提示词：地标名称 + 作品名称 + 集数 + 时间戳 + 坐标
  const landmarkLines = landmarks.map((lm, i) => {
    const parts = [`${i + 1}. 地点名称：${lm.name || lm.originalName || '未知'}`]
    parts.push(`   作品名称：${lm.bangumiName || '未知'}`)
    if (lm.ep) parts.push(`   出现集数：EP${lm.ep}`)
    if (lm.s || lm.s === 0) parts.push(`   时间戳：${formatTimestamp(lm.s)}`)
    if (lm.geo) parts.push(`   坐标：${lm.geo[0]}, ${lm.geo[1]}`)
    return parts.join('\n')
  }).join('\n\n')

  const dataText = `巡礼天数：${days}天\n\n需要访问的地标（共${landmarks.length}个）：\n${landmarkLines}`

  // Cloud / Local 方案 → OpenAI 兼容格式
  const response = await fetch(url, {
    method: 'POST',
    headers: {
      'Content-Type': 'application/json',
      'Authorization': `Bearer ${apiKey}`
    },
    body: JSON.stringify({
      model,
      messages: [{ role: 'user', content: dataText }]   // ← 地标数据作为提示词
    })
  })

  const data = await response.json()
  return data.choices[0].message.content   // ← 模型返回的 Markdown 文本
}
```

---

## 五、Agent 方案 — LangGraph 4 阶段流水线

**文件：** `agent.py`

**描述：** Agent 方案走 LangGraph 多节点流水线，最终由 `supervisor_editor_node` 生成 Markdown 排版。

| 节点 | 行号 | 函数 | 功能 |
|------|------|------|------|
| `info_retriever` | 291-383 | `info_retriever_node` | 解析地标 → Tavily 联网检索 → MiMo 视觉分析，补全每个地标的 `final_info` |
| `routing_specialist` | 385-441 | `routing_specialist_node` | 按地理区域分配天数、查询天气、给出交通动线（输出 JSON 大纲） |
| `anime_expert` | 443-476 | `anime_expert_node` | 注入名场面时段、拍照角度、打卡 Pose 建议（输出 JSON 精排） |
| `supervisor_editor` | 478-524 | `supervisor_editor_node` | 合理性审查（最多 3 轮修正）→ 生成最终 Markdown |

**最终 Markdown 模板** 定义在 `EDITOR_FORMATTING_PROMPT`（`agent.py:184-230`）：

```
# 多作品混合圣地巡礼路线规划（X日版）
## 整体规划说明
- 地标总数：X个
- 涉及作品：xxx
- ...

---
## 第N天：XX区域巡礼（共X个地点）
### 路线顺序：起点站 → 地标1 → 地标2 → … → 返程站
### 当日步行：约Xkm | 总耗时：约X小时

1. 【地标：XX】
- 作品出处：《XX》EPX XX:XX
- 坐标：纬度/经度
- 现实地址：xxx
- 交通衔接：xxx
- 打卡建议：xxx
- ...
```

---

## 六、Store 中的调用入口

**文件：** `src/stores/app.js`

| 方法 | 行号 | 说明 |
|------|------|------|
| `generateItinerary()` | 192-219 | 调用 `planRoute()`（Turf.js 算法），结果写入 `store.itinerary` → 地图画折线 |
| `generateLibraryItinerary()` | 307-341 | 调用 `generateAIRoute()`（LLM），结果写入 `store.libraryAiResponse` → Markdown 渲染 |

**关键设计：** `generateItinerary` 的结果驱动地图绘制；`generateLibraryItinerary` 的结果仅用于文本展示，两路互不干扰。

---

## 对应关系总表

| 描述中的步骤 | 文件 | 函数/代码 |
|-------------|------|-----------|
| Turf.js 空间聚类（按天数分组） | `src/utils/routePlanner.js:22-50` | `turf.clustersKmeans(collection, { numberOfClusters: days })` |
| 最近邻排序（每日内访问顺序） | `src/utils/routePlanner.js:80-121` | `nearestNeighborSort()` |
| 算法入口编排 | `src/utils/routePlanner.js:3-78` | `planRoute(points, days, center)` |
| 拼装提示词 → 发送 LLM | `src/utils/api.js:118-158` | `generateAIRoute()` 中的 `dataText` 拼装 |
| Agent 4 阶段流水线 | `agent.py:291-524` | `info_retriever → routing_specialist → anime_expert → supervisor_editor` |
| Store 调用算法路径 | `src/stores/app.js:192-219` | `generateItinerary()` → 地图折线 |
| Store 调用 LLM 路径 | `src/stores/app.js:307-341` | `generateLibraryItinerary()` → Markdown 展示 |

---

*生成于 2026-06-08，基于项目源代码*
