# 动漫巡礼旅游规划系统 — 技术方案文档

## 一、项目定位

动漫圣地巡礼（Anime Pilgrimage）旅游规划工具。用户搜索动漫作品，获取现实取景地信息，在地图上可视化浏览，收藏地标，并生成多日巡礼行程。

## 二、架构概览

```
┌─────────────────────────────────────────┐
│             前端 Vue 3 SPA                │
│  Vite dev server (port 5173)             │
│  ├── 视图层: App.vue + 5 个组件           │
│  ├── 状态层: Pinia (单一 store)           │
│  └── 数据层: api.js + routePlanner.js    │
├─────────────────────────────────────────┤
│          Python 智能体后端 (可选)           │
│  FastAPI + Uvicorn (port 8000)           │
│  LangChain Agent + DeepSeek + Tavily     │
├─────────────────────────────────────────┤
│              外部数据源                    │
│  Anitabi API (取景地)                     │
│  Bangumi API (作品搜索)                   │
│  Ark API / DeepSeek (LLM)                │
│  Tavily Search (联网工具)                 │
└─────────────────────────────────────────┘
```

## 三、前端架构

### 3.1 组件树（实际运行）

```
App.vue
├── SearchPanel.vue           — 侧边栏顶部
│   ├── 搜索: Bangumi ID 直接查询 / 关键词搜索
│   └── AI 设置对话框: 三种方案切换 + 参数配置
├── CoordinateLibrary.vue     — 侧边栏底部 (两个 tab)
│   ├── 坐标库: 收藏的地标列表 + 全选/删除 + AI生成路线
│   └── 历史记录: 本地存储的路线记录 (可展开加载详情)
└── <main>                    — 主区域
    ├── MapView.vue           — Leaflet 地图
    │   ├── 自定义 DivIcon 标记 (按天着色)
    │   ├── 弹窗: 截图 + 剧情信息 + 收藏/对比按钮
    │   ├── Polyline 路线虚线
    │   ├── 瓦片切换: CartoDB / 高德
    │   └── 图片对比覆盖层 (动画截图 vs 用户拍摄)
    └── LandmarkDock.vue      — 底部浮动横向滚动条
```

**未使用的组件:** `LandmarkList.vue` — 文件存在但未被 App.vue 引用。

### 3.2 状态管理

单一 Pinia Store (`stores/app.js`)，按功能域划分：

| 域 | 核心状态 | 说明 |
|----|---------|------|
| 当前作品 | `bangumi`, `points`, `searchResults` | 从 Anitabi API 获取 |
| 地图交互 | `selectedPointId`, `compareData` | 触发弹窗 / 图片对比 |
| 客户端路线 | `itinerary`, `days` | Turf.js 算法生成 |
| 坐标库 | `coordinateLibrary`, `libraryDays`, `libraryAiResponse` | 跨作品收藏 |
| 历史记录 | `routeHistory` | localStorage 持久化 |
| AI 配置 | `aiConfig` | 3 种方案, localStorage 持久化 |
| UI 通用 | `loading`, `error` | 全局 |

### 3.3 API 层 (`utils/api.js`)

使用 Axios 封装三个外部 API：

| API | 端点 | 用途 |
|-----|------|------|
| Anitabi | `api.anitabi.cn/bangumi/{id}/lite` | 作品概要 (标题、封面、中心坐标) |
| Anitabi | `api.anitabi.cn/bangumi/{id}/points/detail?haveImage=true` | 取景地详情列表 |
| Bangumi | `api.bgm.tv/search/subject/{keyword}?type=2` | 关键词搜索动画作品 |

错误处理: 15s 超时, 404/429/网络异常 → 中文提示。

### 3.4 路线规划 (`utils/routePlanner.js`)

基于 Turf.js 的客户端路线规划：

- **单日**: 最近邻贪心排序 (以作品中心为起点)
- **多日**: K-Means 聚类 (`turf.clustersKmeans`) → 按距中心排序 → 簇内最近邻排序
- 失败回退: 聚类失败时降级为单日全局排序

### 3.5 MapView 实现要点

- **非 vue-leaflet**: 使用原生 `L.map()` 创建地图实例
- **标记**: `L.divIcon` 自定义 HTML, CSS 驱动样式
- **弹窗**: HTML 字符串拼接 (`createPopupContent()`), `LandmarkPopup.vue` 未使用
- **路线**: `L.polyline` 虚线按天着色
- **瓦片**: CartoDB Voyager (默认) + 高德地图 (可切换)
- **图片对比**: 全屏覆盖层, 左右分栏, 独立缩放 (10%–300%)
- **zoom 控件偏移**: `left: 390px` 避让侧边栏

### 3.6 AI 路线生成 (`generateAIRoute`)

三种方案统一入口, 根据 URL 路径区分模式:

1. **云端 (Ark)**: 标准 Chat Completions API, `Authorization: Bearer` 鉴权。Vite 代理 `/ark` → `ark.cn-beijing.volces.com/api/v3`
2. **本地 (Ollama/LM Studio)**: 同 OpenAI 格式, 指向 `localhost:11434`
3. **智能体 (Agent)**: `POST /api/agent/plan`, 发送结构化 JSON, 接收流式响应。Vite 代理 `/api` → `localhost:8000`

数据格式: 将地标信息 (名称、作品、剧集、时间戳、坐标) 拼接为结构化文本 prompt。

## 四、Python 智能体后端

### 4.1 文件结构

| 文件 | 用途 |
|------|------|
| `server.py` | FastAPI 入口, `POST /api/agent/plan` 流式端点 |
| `agent.py` | LangChain Agent 定义 + 系统提示词 |
| `tools.py` | LangChain 工具函数 |
| `pyproject.toml` | 依赖管理 (uv) |

### 4.2 server.py

- FastAPI 应用 + CORS 中间件
- `POST /api/agent/plan`: 接收 `{days, landmarks}` JSON, 调用 `agent.stream()` 返回 `StreamingResponse` (text/plain)
- 启动: `uvicorn server:app --host 127.0.0.1 --port 8000 --reload`

### 4.3 agent.py

```python
# 定义了两个模型, 仅 llm_deepseek 被使用
llm = init_chat_model("google/gemma-4-e4b", base_url="http://localhost:1234/v1")   # 未使用
llm_deepseek = init_chat_model("deepseek-v4-flash", base_url="https://api.deepseek.com")

agent = create_agent(model=llm_deepseek, tools=TOOLS, system_prompt=SYSTEMPROMPT)
```

- 使用 `langchain.agents.create_agent` (LangGraph 实现)
- 模型: `deepseek-v4-flash` (DeepSeek API), 禁用 thinking
- 系统提示词: 中文, 定义巡礼规划师角色, 要求按地理区域分配、控制步行距离、时间还原优先

### 4.4 tools.py

| 工具 | 函数 | 说明 |
|------|------|------|
| `get_weather` | `wttr.in` API | 查询城市实时天气 |
| `get_attraction` | Tavily Search | 搜索景点推荐 (根据城市+天气) |
| `get_anime_scene` | Tavily Search | 查询动漫场景剧情/氛围信息 |
| `get_time` | 硬编码 | 定义但未导出 |
| `get_yg` | 硬编码 | 定义但未导出 |

`TOOLS = [get_weather, get_attraction, get_anime_scene]`

## 五. AI 配置系统

用户在 SearchPanel 的设置对话框中配置, 持久化到 localStorage (`ai-config` key)。

```javascript
{
  scheme: 'cloud',  // 'cloud' | 'local' | 'agent'
  cloud:  { url: '/ark/bots/chat/completions', apiKey: '...', model: '...' },
  local:  { url: 'http://localhost:11434/v1/chat/completions', apiKey: '', model: 'deepseek-r1:7b' },
  agent:  { url: '/api/agent/plan', apiKey: '', model: '' }
}
```

## 六、数据持久化

| 数据 | 存储方式 | 键名 |
|------|---------|------|
| AI 配置 | localStorage | `ai-config` |
| 路线历史 | localStorage | `route-history` |

路线历史结构:
```javascript
{ id, timestamp, days, landmarks: [{id, bangumiId}], aiResponse, scheme }
```

历史记录展开时懒加载: 通过 `fetchBangumiLite` + `fetchBangumiPointsDetail` 获取地标详情。

## 七、已知问题 / 未接入代码

1. `LandmarkList.vue` — 文件存在但未被引用, 可能为旧版遗留
2. `LandmarkPopup.vue` — 文件存在但未被引用, MapView 使用 HTML 字符串构建弹窗
3. `@vue-leaflet/vue-leaflet` — 在 package.json 中声明但未使用
4. `agent.py` 中的 `llm` (Gemma/LM Studio) — 定义但赋值后未被使用
5. `tools.py` 中的 `get_time` 和 `get_yg` — 定义但未导出到 TOOLS
6. 无测试文件

## 八、启动方式

```bash
# 1. 前端
npm run dev

# 2. Python 智能体后端 (可选 - 仅在使用 Agent 方案时需要)
uv run uvicorn server:app --host 127.0.0.1 --port 8000 --reload
```

## 九、依赖

### Node (package.json)
vue 3.4, pinia 2.1, element-plus 2.6, leaflet 1.9, @turf/turf 7.0, axios 1.6, vite 5.1

### Python (pyproject.toml)
fastapi, uvicorn, langchain, langgraph, langchain-openai, tavily, python-dotenv, volcengine-python-sdk, requests

---

*文档版本: v2.0 | 基于代码实际状态生成 | 2026-05-20*
