# 动漫巡礼旅游规划系统 — 技术方案文档

## 一、项目背景与设计动机

### 1.1 问题背景

"圣地巡礼"（Anime Pilgrimage）是 ACGN 文化中的一种旅游形式，指动漫爱好者前往动漫作品中出现的现实取景地进行探访。近年来，随着《你的名字》《孤独摇滚！》《灌篮高手》等作品的热播，圣地巡礼已成为日本及全球动漫文化旅游的重要组成部分。

然而，当前巡礼旅行规划面临以下痛点：

- **信息分散**：取景地信息分布在论坛、Wiki、社交媒体等多个渠道，缺乏统一检索入口。
- **空间认知困难**：取景地以文字描述为主，旅行者难以直观感知各地点之间的空间关系和地理分布。
- **路线规划低效**：手动规划多日巡礼路线需要反复查阅地图、估算距离，耗时且容易遗漏关键地点。
- **缺乏个性化工具**：市面上的通用旅游规划工具不针对巡礼场景优化，无法结合动漫剧情时间线和取景地进行联动规划。

### 1.2 项目目标

本项目设计并实现了一个面向动漫圣地巡礼场景的智能旅游规划单页应用（SPA），核心目标包括：

1. **一站式信息聚合**：集成 Bangumi 作品数据库和 Anitabi 圣地巡礼数据库，实现动漫作品的快速检索与取景地信息的结构化展示。
2. **空间可视化**：基于 Web 地图技术，将取景地的地理坐标以交互式地图形式呈现，支持多图层切换。
3. **智能路线生成**：结合地理空间聚类算法与大语言模型（LLM），为用户自动生成多日巡礼行程规划。
4. **巡礼辅助功能**：提供坐标收藏、截图实地对比、历史记录管理等巡礼全流程支持。

---

## 二、技术栈

### 2.1 前端框架与构建工具

| 技术 | 版本 | 用途 |
|------|------|------|
| Vue 3 | ^3.4.21 | 前端 UI 框架，采用 Composition API + `<script setup>` 语法 |
| Vite | ^5.1.6 | 构建工具与开发服务器，支持 ESM 原生开发体验 |
| Pinia | ^2.1.7 | 状态管理库，Vue 3 官方推荐方案 |
| Element Plus | ^2.6.1 | UI 组件库，提供输入框、弹窗、标签页等通用 UI 组件 |

### 2.2 地图与地理空间技术

| 技术 | 版本 | 用途 |
|------|------|------|
| Leaflet | ^1.9.4 | 开源 Web 地图渲染引擎 |
| @vue-leaflet/vue-leaflet | ^0.10.1 | Leaflet 的 Vue 3 封装 |
| Turf.js (@turf/turf) | ^7.0.0 | 地理空间分析库，提供 K-Means 聚类、球面距离计算等 |

### 2.3 网络与 API 通信

| 技术 | 版本 | 用途 |
|------|------|------|
| Axios | ^1.6.7 | HTTP 客户端，用于调用外部 REST API |

### 2.4 AI / 大语言模型

| 服务 | 说明 |
|------|------|
| 火山引擎 Ark API | 默认云端 LLM 方案，通过 Vite 开发代理转发请求 |
| LM Studio 本地方案 | 可选本地 LLM 方案，支持 DeepSeek-R1 等开源模型 |

### 2.5 开发辅助工具

| 技术 | 版本 | 用途 |
|------|------|------|
| unplugin-vue-components | ^0.26.0 | Element Plus 组件按需自动导入 |
| unplugin-auto-import | ^0.17.5 | Vue API 自动导入 |

---

## 三、系统架构设计

### 3.1 总体架构

系统采用典型的单页应用（SPA）三层架构：

```
┌─────────────────────────────────────────────────┐
│                   视图层 (View)                    │
│  App.vue  →  SearchPanel / MapView / Dock 等组件  │
├─────────────────────────────────────────────────┤
│                 状态管理层 (Store)                  │
│          Pinia Store (app.js) — 单一数据源          │
├─────────────────────────────────────────────────┤
│               数据访问层 (Data Access)              │
│    api.js (Anitabi/Bangumi/Ark)                  │
│    routePlanner.js (Turf.js 聚类与排序)            │
└─────────────────────────────────────────────────┘
```

数据在各层之间单向流动：外部 API → 数据访问层 → Pinia Store → Vue 组件。组件之间不通过 props/emits 直接通信，所有状态变更通过 Store 中转。

### 3.2 组件树结构

```
App.vue
├── SearchPanel.vue              (侧边栏顶部 — 作品搜索与 AI 配置)
├── CoordinateLibrary.vue        (侧边栏底部 — 坐标库与历史记录)
└── <main>                       (主区域)
    ├── MapView.vue              (Leaflet 地图 — 标注、路线、截图对比)
    └── LandmarkDock.vue         (底部浮动横栏 — 地标快速浏览)
```

布局采用经典侧边栏 + 主区域方案，侧边栏固定宽度 360px，主区域弹性填充剩余空间。

### 3.3 状态管理设计

系统使用单一 Pinia Store（`stores/app.js`），按功能域组织状态：

| 状态域 | 核心字段 | 说明 |
|--------|---------|------|
| 作品信息 | `bangumi`, `searchResults`, `searching` | 当前加载的动漫作品元数据与搜索结果 |
| 地标数据 | `points`, `selectedPointId` | 当前作品的取景地坐标点 |
| 路线规划 | `itinerary`, `days` | 算法生成的路线结果与天数配置 |
| 坐标库 | `coordinateLibrary`, `libraryDays`, `libraryAiResponse` | 用户收藏的地标与 AI 路线结果 |
| 历史记录 | `routeHistory` | 持久化到 localStorage 的路线历史 |
| 截图对比 | `compareData` | 触发地图组件进入截图对比模式的信号量 |
| AI 配置 | `aiConfig` | LLM 连接配置（云端/本地切换，持久化至 localStorage） |
| UI 状态 | `loading`, `error` | 全局加载与错误状态 |

持久化策略：AI 配置与路线历史通过 `localStorage` 手动序列化/反序列化，无需额外持久化插件。

---

## 四、核心技术路线

### 4.1 数据获取流程

系统集成了两个外部开放 API 作为数据源：

#### 4.1.1 Anitabi API（取景地数据库）

Anitabi（anitabi.cn）是一个众包维护的动漫圣地巡礼数据库，提供动漫作品与其对应现实取景地的结构化数据。

**接口调用：**

- **作品概要**：`GET https://api.anitabi.cn/bangumi/{subjectID}/lite`
  - 返回：作品标题、封面图、地理中心坐标（geo）、默认缩放级别（zoom）
- **取景地详情**：`GET https://api.anitabi.cn/bangumi/{subjectID}/points/detail?haveImage=true`
  - 返回：所有取景地点位数组，每个点包含经纬度、名称、出现剧集、时间戳、动画截图 URL、来源网站链接

#### 4.1.2 Bangumi API（作品检索）

Bangumi（bgm.tv）是华语 ACGN 社区的核心数据库，提供动漫作品的元数据检索。

**接口调用：**

- **关键词搜索**：`GET https://api.bgm.tv/search/subject/{keyword}?type=2&responseGroup=small&max_results=10`
  - `type=2` 限定结果类型为动画
  - 返回匹配作品列表（含 subjectID、中文名、日文名、封面等）

#### 4.1.3 数据获取安全机制

- Axios 请求配置 15 秒超时
- 自定义错误拦截器，将 HTTP 状态码（404、429）和网络异常转换为用户可读的中文提示
- 所有外部 API 无需认证，LLM API 通过 Bearer Token 鉴权

### 4.2 地图可视化方案

#### 4.2.1 地图引擎选型

选择 Leaflet 而非重量级方案（如 Mapbox GL JS、百度地图 SDK），理由如下：

- **轻量级**：Leaflet 核心库约 40KB（gzip），对移动端友好
- **开源免费**：无需商业授权或 API Key（瓦片服务可自由更换）
- **生态成熟**：插件丰富，支持自定义 DivIcon、LayerGroup 等高级特性
- **双瓦片源支持**：系统提供 CartoDB Voyager（国际风格）和高德地图（中国风格）两种可切换瓦片图层

#### 4.2.2 标记系统

使用 Leaflet 的 `L.divIcon` 自定义 HTML 标记，通过 CSS 类名实现按天着色：

- 颜色方案：7 天分别使用 `#409EFF`、`#67C23A`、`#E6A23C`、`#F56C6C`、`#909399`、`#b37feb`、`#36cfc9`
- 选中标记：30px 尺寸、全不透明度、白色边框 + 阴影
- 未选中标记：22px 尺寸、0.3 不透明度、无阴影
- 标记内容为数字索引，对应路线规划中的顺序

#### 4.2.3 路线渲染

路线通过 `L.polyline` 以虚线样式（dashArray: "8,6"）绘制，颜色与每天标记颜色一致，按标记在地图上的顺序依次连线。路线数据来源于 AI 生成的顺序结果，而非实时路径导航。

#### 4.2.4 边界自适应

使用 `L.latLngBounds` 计算所有标记点的外接矩形，调用 `map.fitBounds` 使地图视口自动适配所有点，padding 设为 40px；单点时回退为固定缩放级别 14 的中心视图。

### 4.3 路线规划算法（客户端）

#### 4.3.1 算法设计

路线规划模块（`utils/routePlanner.js`）基于 Turf.js 实现了一种**贪心聚类 + 最近邻排序**的混合算法。

**输入**：用户选中的取景地点位集合 `P = {p_1, p_2, ..., p_n}`，计划天数 `days`。

**输出**：按天分组的点位序列 `I = [day_1, day_2, ..., day_days]`，每一天内包含一个有序点位列表。

**算法步骤：**

**(a) 单日情况（days = 1）：**

直接对所有点执行最近邻排序。以作品地理中心点为起点，贪心选择距离当前点最近的未访问点，迭代直至所有点排序完成。本质上是旅行商问题（TSP）的贪心近似解。

**(b) 多日情况（days > 1）：**

1. **空间聚类**：使用 Turf.js 的 `clustersKmeans()` 函数将点位集 `P` 划分为 `days` 个聚类。K-Means 是一种基于欧氏距离的硬聚类算法，在此处将地理坐标投影到二维平面进行聚类。
2. **聚类排序**：计算每个聚类的中心坐标（经纬度平均），按距作品中心点的距离升序排列聚类。距离最近聚类 → 第 1 天，以此类推。
3. **簇内排序**：在每个聚类内部，以作品中心点为起点执行最近邻排序，得到当天内的推荐访问顺序。
4. **异常处理**：当 K-Means 聚类失败（如特殊边缘情况或全部点重合）时回退为单日全局排序。

#### 4.3.2 距离计算

使用 Turf.js 的 `turf.distance()` 计算球面大圆距离（Great-circle distance），基于 Haversine 公式，考虑地球曲率。路线总距离为连续点对距离累加，四舍五入保留两位小数（公里）。

#### 4.3.3 复杂度分析

- 最近邻排序：时间复杂度 O(n²)，空间复杂度 O(n)
- K-Means 聚类：Turf.js 内部使用迭代优化，时间复杂度 O(days × n × iterations)
- 整体复杂度在 n ≤ 50、days ≤ 7 的典型场景下运算时间可忽略不计

### 4.4 大语言模型路线生成

#### 4.4.1 设计思路

纯算法的路线规划基于地理空间距离优化，但无法考虑以下因素：

- 取景地的"圣地等级"（如名场面 vs 背景板）
- 不同作品之间的剧情关联
- 交通可达性（某些点虽近但无公共交通连接）
- 时间安排合理性（如某些取景地适合傍晚拍摄）

因此系统引入了 LLM 作为辅助路线规划手段，利用大语言模型的理解和推理能力生成更具"巡礼体验"价值的行程安排。

#### 4.4.2 提示词工程

发送给 LLM 的提示词包含以下结构化信息：

```
你是一位专业的动漫圣地巡礼规划师。请为用户规划一个 {days} 天的巡礼路线。

以下是所有待巡礼的地点：
- 地点名称：{name}
  所属作品：{bangumiTitle}
  出现剧集：第 {ep} 集
  时间戳：{timestamp}
  坐标：(纬度, 经度)
  ...
```

要求 LLM 综合考虑地理邻近性、作品热门程度、巡礼体验节奏后输出按天分组的行程文本。

#### 4.4.3 API 调用架构

系统支持双 LLM 方案，通过前端面板可随时切换：

- **云端方案（默认）**：火山引擎 Ark API，通过 Vite 开发代理（`/ark` → `https://ark.cn-beijing.volces.com/api/v3`）转发请求，使用 Bot 模型端点
- **本地方案**：LM Studio 本地推理服务，兼容 OpenAI Chat Completions 接口格式，默认模型 `deepseek-r1:7b`

两种方案共享统一的请求格式（`Authorization: Bearer {token}`、JSON body 含 `model` 和 `messages`），通过配置对象的 `url`、`apiKey`、`model` 字段区分。

#### 4.4.4 结果处理

AI 返回的 Markdown 格式行程文本在前端经 `renderMarkdown()` 函数渲染为可读 HTML（支持标题、粗体、有序/无序列表、段落），并支持一键复制到剪贴板。同时，路线坐标顺序信息传递给地图组件以绘制空间路径。

### 4.5 截图实地对比功能

系统提供一种创新的"截图 vs 实地"并排对比模式：

- 左侧显示动漫原版截图（来自 Anitabi API）
- 右侧支持用户上传现场拍摄照片（通过 `FileReader.readAsDataURL` 读取）
- 每张图片具有独立的缩放控制（滚轮 + 加减按钮，范围 10%–300%）
- 全屏覆盖层呈现，便于现场巡礼时进行精确构图比对

---

## 五、技术路线图

```
                    用户输入
                       │
                       ▼
            ┌──────────────────────┐
            │   SearchPanel.vue     │  ← Bangumi 关键词搜索 / Bangumi ID 查询
            └──────────┬───────────┘
                       │ subjectID
                       ▼
            ┌──────────────────────┐
            │  Anitabi API          │  ← 获取取景地点位数据
            │  (anitabi.cn)         │
            └──────────┬───────────┘
                       │ 点位数据 (geo + 截图 + 剧集信息)
                       ▼
            ┌──────────────────────┐
            │   Pinia Store         │  ← 集中状态管理
            │   (app.js)           │
            └──┬────────┬─────────┘
               │        │
               ▼        ▼
    ┌──────────────┐  ┌─────────────────┐
    │  MapView.vue  │  │ ItineraryPanel   │
    │  (Leaflet)   │  │ + LandmarkList   │
    └──────┬───────┘  └────────┬────────┘
           │                    │
           │   用户勾选点位      │
           │   设置天数          │
           │                    ▼
           │         ┌─────────────────────┐
           │         │  路线规划引擎         │
           │         ├─────────────────────┤
           │         │ 客户端: Turf.js      │
           │         │ K-Means + 最近邻     │
           │         ├─────────────────────┤
           │         │ 服务端: LLM (Ark/    │
           │         │ LM Studio)           │
           │         └──────────┬──────────┘
           │                    │
           ▼                    ▼
    ┌──────────────────────────────────┐
    │         路线可视化 (Polyline)       │
    │    + 按天着色标记 + 弹窗详情         │
    └──────────────────────────────────┘
```

---

## 六、关键技术特性总结

### 6.1 技术亮点

1. **双路线规划引擎**：客户端 K-Means 聚类算法与 LLM 智能规划互补，兼顾效率和体验质量。
2. **无需后端服务器**：纯前端 SPA 架构，外部 API 调用通过 Vite 代理解决跨域问题，部署成本低（静态文件托管即可）。
3. **LLM 方案灵活切换**：支持云端大模型和本地开源模型两套方案，适应不同网络环境和成本要求。
4. **自定义地图标记系统**：基于 `L.divIcon` 的 CSS 驱动标记，按天颜色编码 + 选中状态差异化的视觉设计。
5. **截图对比模式**：独立缩放控制的双图并排对比，直接服务于圣地巡礼的核心体验——"还原动画场景"。
6. **数据持久化**：AI 配置、路线历史通过 localStorage 持久化，支持离线查看历史记录；历史记录展开时懒加载 API 数据。

### 6.2 系统局限与改进方向

1. **路线可视化不支持真实路网导航**：当前路线为直线连接，未接入道路网络数据。未来可集成 OSRM 或高德路径规划 API 实现真实路网导航。
2. **标记量大时性能下降**：当前无标记聚类（如 MarkerCluster），在点位 > 100 时建议引入聚类插件或虚拟化渲染。
3. **AI 路线结果非结构化**：LLM 输出为 Markdown 文本，地图渲染依赖文本解析生成坐标序列，存在解析失败风险。未来可优化为要求 LLM 输出结构化 JSON。
4. **单一 Store 文件较大**：随着功能增长，可考虑按功能域拆分为多个 Pinia Store 模块。

---

## 七、参考文献与技术资料

### 技术官方文档

- Vue 3: https://vuejs.org/
- Vite: https://vitejs.dev/
- Pinia: https://pinia.vuejs.org/
- Element Plus: https://element-plus.org/
- Leaflet: https://leafletjs.com/
- Turf.js: https://turfjs.org/

### 外部数据源

- Anitabi 圣地巡礼数据库: https://anitabi.cn/
- Bangumi 番组计划: https://bgm.tv/

### AI 服务平台

- 火山引擎 Ark API: https://www.volcengine.com/product/ark
- LM Studio: https://lmstudio.ai/

---

*文档版本：v1.0 | 生成日期：2026-05-17*
